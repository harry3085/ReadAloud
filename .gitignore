# Vercel
.vercel

# 🔐 환경변수 파일 — 절대 업로드 금지
.env
.env.local
.env*.local
.env.development
.env.production

# 서비스 계정 JSON 키 — 절대 업로드 금지
*service-account*.json
*-credentials.json
google-vision-key.json
scripts/.firebase-admin-key.json

# Firestore 로컬 백업 (용량 큼 + 민감 데이터)
backups/

# 🆕 Node 의존성 (용량 커서 GitHub 올리면 안 됨)
node_modules/

# 🆕 Claude Code 로컬 설정 (올릴 필요 없음)
.claude/


# OS/에디터 파일
.DS_Store
Thumbs.db
.vscode/
.idea/

# 로그
*.log
npm-debug.log*

# 백업/임시 파일
*.bak
*.backup
*.old
_scratch/

# 로컬 테스트 페이지
public/ocr-test.html

# Visual Studio IDE 폴더
.vs/

# 임시 JSON / CLAUDE.md 백업 (분리 작업 산출물)
*-tmp.json
CLAUDE.md.backup-*

# 로컬 API 테스트 (평문 키 사고 재발 방지 — 2026-05-23)
test-*.ps1

# 일회용 진단 스크립트 (그때그때 만들어 쓰는 도구, repo 보관 X)
scripts/diag/